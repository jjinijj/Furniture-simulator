# 📐 구면좌표계 수학 공식 가이드

**작성일:** 2024년 12월 12일  
**목적:** 카메라 컨트롤러의 수학적 원리 이해

## 🎯 핵심 공식

### 구면좌표 → 직교좌표

```
X = r × sin(φ) × sin(θ)
Y = r × cos(φ)
Z = r × sin(φ) × cos(θ)

r = 거리 (radius)
θ = 수평 각도 (yaw)
φ = 수직 각도 (pitch)
```

---

## 🎮 Unity 적용

```csharp
void UpdateCameraPosition()
{
    // 1. 도 → 라디안 변환
    float yawRad = currentYaw * Mathf.Deg2Rad;
    float pitchRad = currentPitch * Mathf.Deg2Rad;
    
    // 2. 구면좌표 → 직교좌표
    Vector3 offset = new Vector3(
        currentDistance * Mathf.Sin(pitchRad) * Mathf.Sin(yawRad),  // X
        currentDistance * Mathf.Cos(pitchRad),                       // Y
        currentDistance * Mathf.Sin(pitchRad) * Mathf.Cos(yawRad)   // Z
    );
    
    // 3. 최종 위치
    transform.position = targetPosition + offset;
    transform.LookAt(targetPosition);
}
```

---

## 📊 시각적 이해

```
         Y (위)
         ↑
         |
         | 📷 카메라
         |/|
        / |
       /  | r
      /   |
     /φ   |
    /_____|_____ (수평면)
   O      
   |
   └────────► X (오른쪽)
  /
 Z (앞)
```

---

## 🎯 주요 용어

- **r (radius):** 중심에서의 거리
- **θ (theta):** 수평 각도 (Yaw, 좌우 회전)
- **φ (phi):** 수직 각도 (Pitch, 상하 각도)

---

## 💡 장점

```
✅ 회전 표현 간단
✅ 거리 일정 유지 자동
✅ 오빗 카메라에 최적
```

---

## ⚠️ 주의사항

### 짐벌락 방지

```csharp
// φ 값 제한으로 짐벌락 방지
currentPitch = Mathf.Clamp(currentPitch, 5f, 175f);
```

### 각도 단위 변환

```csharp
// 도 → 라디안
float radians = degrees * Mathf.Deg2Rad;

// 라디안 → 도
float degrees = radians * Mathf.Rad2Deg;
```

전체 상세 가이드는 프로젝트 루트의 `구면좌표계_수학_공식_가이드.md` 참조.
