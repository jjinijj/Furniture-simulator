# 📝 Git 커밋 메시지 가이드

**작성일:** 2025년 12월 12일

## 추천 스타일

### 일상적 커밋 (간단)

```bash
git commit -m "Add furniture rotation"
```

### 마일스톤 커밋 (중간) ⭐ 추천

```bash
git commit -m "Complete Week 2 Day 5: Furniture rotation

- Add Q/E rotation for selected furniture
- Unify rotation input handling
- Fix code style issues
"
```

## 기본 규칙

```
1. 제목: 50자 이하
2. 명령형 동사 (Add, Fix, Update)
3. 첫 글자 대문자
4. 마침표 없음
5. 제목과 본문 사이 빈 줄
```

## 좋은 예시

```bash
# ✅ 명확
git commit -m "Add Q/E rotation for furniture"

# ✅ 구체적
git commit -m "Fix collision detection bug"

# ✅ 본문 포함
git commit -m "Complete Week 2 Day 5

- Add rotation feature
- Fix bugs
- Update documentation
"
```

## 나쁜 예시

```bash
# ❌ 모호
git commit -m "Update code"

# ❌ 과거형
git commit -m "Added rotation"

# ❌ 소문자
git commit -m "add rotation"
```

전체 상세 가이드는 프로젝트 루트의 `Git_커밋_메시지_가이드.md` 참조.
