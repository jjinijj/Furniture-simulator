# 🌿 Git 브랜치 전략 가이드

**프로젝트:** Furniture Simulator  
**작성일:** 2025년 12월 12일

## 기본 브랜치 구조

```
main (배포용, 안정 버전)
  │
  ├── feature/week1-furniture-assets
  ├── feature/week2-placement-system
  ├── feature/week2-furniture-rotation
  └── ...
```

## 브랜치 네이밍 규칙

```
feature/[주차]-[기능명]
bugfix/[버그명]
hotfix/[긴급수정]
refactor/[리팩토링명]
docs/[문서명]
```

## 기본 워크플로우

### 새 기능 시작

```bash
git checkout main
git pull origin main
git checkout -b feature/week2-furniture-rotation
```

### 작업 & 커밋

```bash
git add .
git commit -m "Add furniture rotation"
git push -u origin feature/week2-furniture-rotation
```

### 완료 후 병합

```bash
git checkout main
git merge feature/week2-furniture-rotation
git push origin main

# 브랜치 삭제 (선택)
git branch -d feature/week2-furniture-rotation
```

## 자주 쓰는 명령어

```bash
# 브랜치 확인
git branch
git branch -a

# 브랜치 이동
git checkout <브랜치명>

# 브랜치 삭제
git branch -d <브랜치명>  # 로컬
git push origin --delete <브랜치명>  # 원격
```

전체 상세 가이드는 프로젝트 루트의 `Git_브랜치_전략_가이드.md` 참조.
